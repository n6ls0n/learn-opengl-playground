// Question: Create two shader programs where the second program uses a different fragment shader that outputs the color yellow; draw both triangles again where one outputs the color yellow and the other outputs the color orange. The yellow triangle should point to the left and the red triangle should point to the right.

#include <glad/glad.h>
#include <GLFW/glfw3.h>

#include <iostream>
